# CODEX1: отчет исполнителя и актуальный backlog

Дата обновления: 2026-06-12.

Роли:

- `CODEX1` - исполнитель, этот файл.
- `CODEX2` - ревьювер, файл `CODEX2.md`.

Важно:

- `CODEX2.md` не редактировать: это файл ревьювера.
- `CLAUDE.md` локально изменен не CODEX1, не трогать без отдельного решения.
- Excel-выгрузки не делаем как цель. Для документов оставляем HTML-preview и PDF.
- Согласованная логика ниже считается верной. Главная задача ревьюера - проверить качество исполнения, регрессии и полноту тестов.

## Выполнено в текущей итерации

### 1. RS107L/RS107R для 2 рядов

Статус: реализовано.

Файл:

- `Raluma/backend/engine/slide_calc.py`
- `Raluma/backend/tests/test_slide_calc.py`

Что сделано:

- Для 2-рядного слайда убрана старая формула `round(inter_glass_cnt / 2)`.
- Новая формула:

```text
plug_qty_per_side = (panels // 2 - 1) * quantity
```

- Для поддерживаемых межстекольных профилей теперь добавляются обе стороны: `RS107L` и `RS107R`.
- Количество больше не зависит от `unused_track`.
- Отдельное поле `1-я панель слева/справа` для 2 рядов не возвращалось.

Покрыто тестами:

- `P=4, Q=1` -> `1 RS107L + 1 RS107R`.
- `P=6, Q=1` -> `2 + 2`.
- `P=8, Q=1` -> `3 + 3`.
- `P=10, Q=1` -> `4 + 4`.
- `P=4, Q=2` -> `2 + 2`.
- `unused_track="Внешний"` и `unused_track="Внутренний"` дают одинаковое количество.
- `inter_glass_profile="Без"` и `h-профиль RS1004` не создают `RS107L/R`.

Что важно проверить ревьюеру:

- Не сломалась 1-рядная логика `RS107L/R`.
- В производственном листе 2 рядов отображаются обе позиции.
- Нет скрытой зависимости от направления первых панелей.

### 2. Нарезка порога и верхнего направляющего больше 5950 мм

Статус: реализовано.

Файл:

- `Raluma/backend/engine/slide_calc.py`
- `Raluma/backend/tests/test_slide_calc.py`

Что сделано:

- Добавлен лимит `MAX_PROFILE_LENGTH_MM = 5950`.
- Добавлен helper `_split_profile_lengths()`.
- Добавлен helper `_append_profile_with_splits()`.
- Для порога и верхнего направляющего в 1 ряду и 2 рядах теперь:
  - длина `<= 5950` остается одной позицией;
  - длина `> 5950` режется на 2+ частей;
  - каждая часть `<= 5950`;
  - дробная длина нормализуется вверх до целого миллиметра через `ceil`;
  - сумма частей равна целой нормализованной длине;
  - при `quantity > 1` каждая часть получает то же `qty`.

Покрыто тестами:

- `5950` не режется.
- `5950.4` нормализуется до `5951` и режется как `2975 + 2976`.
- `6123 -> 3061 + 3062`.
- `7000 -> 3500 + 3500`.
- `7001 -> 3500 + 3501`.
- `12000 -> 4000 + 4000 + 4000`.
- Проверено, что 2-рядная ветка использует ту же нарезку.
- Проверено, что `quantity=2` не ломает сумму.

Что важно проверить ревьюеру:

- В производственном листе длинные пороги/верхние направляющие видны отдельными строками.
- Обычные секции шириной около `2000` не изменились.
- Не были затронуты остальные профили.

### 3. Саморез DIN7504M

Статус: реализовано.

Файл:

- `Raluma/backend/engine/slide_calc.py`
- `Raluma/backend/tests/test_slide_calc.py`

Что сделано:

- Старое `lb_count * 7 * Q` заменено на расчет от высоты.
- Новая формула в 1-рядной и 2-рядной ветке:

```text
DIN7504M = RU005 * 2 + ceil((H - 200) / 300) * количество_сторон_RS2081 * Q
```

- На ролики формула осталась прежней: `RU005 * 2`.
- Для `H=2400` получается `8` саморезов на одну сторону `RS2081`.
- Если `RS2081` стоит с двух сторон, получается `16` саморезов на `RS2081`, плюс саморезы на ролики.

Покрыто тестами:

- Без `RS2081` -> только ролики.
- Один `RS2081`, `H=2400` -> ролики + 8.
- Два `RS2081`, `H=2400` -> ролики + 16.
- Два `RS2081`, `H=3000` -> расчет меняется по высоте.
- `quantity=2` умножает часть по `RS2081`.

Что важно проверить ревьюеру:

- Не перепутаны `DIN7504M` и `DIN7504O`.
- Не изменена логика саморезов для `RS1082`.
- В 1 ряду и 2 рядах формула одинаково применена.

### Дополнение: закрытие замечаний ревью

Статус: реализовано после ревью.

Файлы:

- `Raluma/backend/engine/slide_calc.py`
- `Raluma/backend/tests/test_slide_calc.py`
- `pytest.ini`
- `conftest.py`

Что сделано:

- Edge-case распила `5950.4` закрыт: `_split_profile_lengths()` больше не использует `round(length_mm)` для решения, резать или нет.
- Длина профиля нормализуется через `ceil(length_mm)`, поэтому дробная длина выше лимита не пройдет одним куском больше `5950`.
- Добавлен тест на `width=5982.4`, где чистая длина порога/верхнего направляющего равна `5950.4`.
- Добавлен корневой `pytest.ini`, чтобы `pytest` из корня репозитория запускал только тесты `Raluma/backend/tests` и не цеплял временные тесты `appglass/scraper/tmp_*_test.py`.
- В `pytest.ini` пути исправлены на регистр из Git: `Raluma/backend/tests` и `Raluma/backend`.
- Добавлен корневой `conftest.py`: он подключает `Raluma/backend` в `sys.path` и переиспользует backend-фикстуры `client`, `admin_headers`, `project`, `section`.
- Причина `conftest.py`: в текущем Windows checkout физическая папка отображается как `raluma`, а Git хранит `Raluma`; без корневого подключения фикстур `pytest -q` из корня видел тесты, но часть тестов не видела фикстуру `client`.

Что важно проверить ревьюеру:

- Для целых размеров поведение распила не изменилось.
- Для дробных размеров сумма частей равна целому значению после нормализации вверх.
- Корневой `pytest` больше не падает на `appglass`.

### 4. `(чертеж)` в заказе стекла

Статус: реализовано.

Файлы:

- `Raluma/backend/engine/project_documents.py`
- `Raluma/backend/tests/test_documents.py`

Что сделано:

- Заказ стекла больше не ставит `(чертеж)` на всю секцию через общий `_has_glass_drawing(section)`.
- Добавлен слой физических стекол для заказа: расчетные агрегированные строки сначала разворачиваются до панелей, потом снова группируются по `(glass_type, width, height, note)`.
- Для 1 ряда:
  - левая боковая фурнитура помечает левое стекло;
  - правая боковая фурнитура помечает правое стекло;
  - промежуточные стекла не получают `(чертеж)` от боковой фурнитуры;
  - старые общие поля `lock/handle` оставлены fallback-ом для крайних/одиночного стекла.
- Для 2 рядов:
  - боковая фурнитура помечает только боковые стекла;
  - центральная фурнитура (`center_lock`, `center_handle`, центральные защелки) помечает только центральные стекла;
  - левая/правая центральная напольная защелка помечает только соответствующее центральное стекло, если нет общего центрального замка/ручки;
  - промежуточные стекла не получают `(чертеж)` от боковой или центральной фурнитуры.
- Backend явно игнорирует no-op строки фурнитуры вроде `Без замка`, `Без ручки`, `Без ...`, `Нет ...`, чтобы старые/API-данные не создавали ложный `(чертеж)`.
- Маркировка осталась в формате `номер проекта секция,стекло`, но теперь номер идет по физическим стеклам внутри секции.

Покрыто тестами:

- 1 ряд, агрегированная строка `Крайние` с `qty=2`: если замок только слева, `(чертеж)` получает только одно левое стекло, правое и промежуточное остаются без note.
- 2 ряда, все стекла одинакового размера: центральный замок делит одинаковые стекла на две строки - центральные с `(чертеж)` и боковые без note.
- 2 ряда, включена только левая центральная напольная защелка: `(чертеж)` получает только левое центральное стекло.
- Строки `Без замка`/`Без ручки` не создают `(чертеж)`.

Что важно проверить ревьюеру:

- Одинаковые стекла с чертежом и без чертежа не объединяются в одну строку.
- `(чертеж)` не размазывается на всю секцию.
- Если в будущем появится фурнитура, которая относится к конкретному промежуточному стеклу, ее надо будет добавить в этот же слой физического разворачивания.

## Проверки текущей итерации

Выполнено:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan\Raluma\backend
pytest -q tests\test_slide_calc.py::TestScrews tests\test_slide_calc.py::TestLongProfileCuts tests\test_slide_calc.py::TestSlideTwoRows
```

Результат:

```text
32 passed
```

Выполнено после ревью:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan\Raluma\backend
pytest -q tests\test_slide_calc.py::TestLongProfileCuts
```

Результат:

```text
7 passed
```

Выполнено:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan\Raluma\backend
pytest -q
```

Результат:

```text
192 passed, 282 warnings
```

Warnings старые, про `datetime.utcnow()` и сторонние места. К текущим правкам не относятся.

Выполнено после ревью:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan
pytest --collect-only -q
pytest -q
pytest -q Raluma\backend\tests
```

Результат:

```text
192 tests collected
192 passed, 282 warnings
192 passed, 282 warnings
```

Корневой `pytest` больше не собирает временные тесты из `appglass`.

Выполнено после правки заказа стекла:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan
pytest -q Raluma\backend\tests\test_documents.py
ruff check Raluma\backend\engine\project_documents.py Raluma\backend\tests\test_documents.py
ruff format --check Raluma\backend\engine\project_documents.py Raluma\backend\tests\test_documents.py
pytest -q
```

Результат:

```text
28 passed, 85 warnings
All checks passed
2 files already formatted
194 passed, 282 warnings
```

Выполнено после ревью замечаний по центральным защелкам и `Без замка`:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan
pytest -q Raluma\backend\tests\test_documents.py::TestProjectGlassOrder
ruff check Raluma\backend\engine\project_documents.py Raluma\backend\tests\test_documents.py
ruff format --check Raluma\backend\engine\project_documents.py Raluma\backend\tests\test_documents.py
pytest -q
```

Результат:

```text
4 passed
All checks passed
2 files already formatted
196 passed, 281 warnings
```

Дополнение по Linux/CI:

- В `pytest.ini` пути указаны с тем же регистром, что и в Git: `Raluma/backend/tests` и `Raluma/backend`.
- На Windows нижний регистр проходил бы, но на Linux мог бы сломать корневой запуск тестов.
- Корневой `conftest.py` оставлен минимальным: он не дублирует fixture-код, а импортирует уже существующие backend-фикстуры.

Выполнено:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan\Raluma\backend
ruff check .
ruff format --check engine\slide_calc.py tests\test_slide_calc.py
```

Результат:

```text
All checks passed
2 files already formatted
```

Отдельное замечание:

- Форматирование применялось только к файлам текущей задачи: `project_documents.py` и `test_documents.py`.
- Массовый формат всего backend не делался, чтобы не смешивать чужие форматные изменения с текущей задачей.

Выполнено:

```powershell
cd C:\Users\Vadim\PycharmProjects\mamajan\Raluma
npm.cmd run typecheck
npm.cmd run lint
npm.cmd run build
```

Результат:

- `typecheck` прошел.
- `lint` прошел без ошибок, остались 14 существующих warnings во frontend.
- `build` прошел, остался стандартный warning Vite про chunk больше 500 kB.

## Актуальный backlog: согласовано, но отложено

## Новый пакет правок из `ПРАВКИ 12.06.26.docx`

Статус: пакет правок в работе. Пункт 7 про форму секции реализован, остальные пункты остаются в плане.

Источник:

- Скрины и подписи из файла `ПРАВКИ 12.06.26.docx`.
- Дополнительное уточнение пользователя: выбор шаблонов секций сверху страницы пока не делаем.

Что НЕ делаем в этом пакете:

- Не делаем выбор готовых шаблонов секций с картинками `2/3/4/5 панелей` и `4/6/8/10/12 панелей`.
- Не переделываем весь каталог в полноценный источник истины в рамках этой маленькой пачки, только используем уже имеющиеся данные каталога там, где они уже подключены.
- Первый кодовый шаг выполнен: убран переключатель `1 ряд / 2 ряда` из формы редактирования секции.

### План выполнения

#### 1. Производственный лист: убрать лишнее слово из цвета

Проблема:

- В строке `ЦВЕТ` сейчас выводится `RAL 9016 МАТОВЫЙ СТАНДАРТ/НЕСТАНДАРТ`.
- Нужно показывать без `СТАНДАРТ/НЕСТАНДАРТ`, например `RAL 9016 МАТОВЫЙ`.

Ожидаемая правка:

- Исправить формирование `calc.color_text` в `Raluma/backend/engine/slide_calc.py` для 1 ряда и 2 рядов.
- Оставить различие `RAL стандарт`/`RAL нестандарт` в данных секции, но не выводить это слово в производственном листе.

Проверки:

- Unit-тест на `RAL стандарт`.
- Unit-тест на `RAL нестандарт`.
- Проверить, что `Анодированный` не сломался.

#### 2. Производственный лист: если межстекольного профиля нет, не показывать строку

Проблема:

- Если межстекольный/промежуточный профиль равен `0` или не выбран, его не должно быть в нарезке профиля.

Ожидаемая правка:

- Проверить расчет в `slide_calc.py`: позиция межстекольного профиля должна добавляться только если выбран реальный профиль и есть количество.
- Проверить шаблон `section_sheet.html`: строки с пустой/нулевой длиной и `qty=0` не должны отображаться.

Проверки:

- Тест: `inter_glass_profile` пустой/без профиля -> `RS2061/RS1006/RS1004` отсутствуют в `calc.profiles`.
- Тест производственного листа: артикул межстекольного профиля не попадает в HTML.

#### 3. Производственный лист: подпись про неиспользуемую полосу

Проблема:

- Для 1 ряда, если есть неиспользуемая полоса/рельс, в листе нужно явно писать, какая именно полоса не используется.
- На скрине отмечено: не просто схема, а текстовое пояснение.

Ожидаемая правка:

- Добавить человекочитаемый текст в расчет или helper шаблона:
  - `Не используется внутренняя полоса`
  - `Не используется внешняя полоса`
- Показывать только когда это применимо: 1 ряд и количество панелей меньше количества рельсов.

Проверки:

- 3 рельса, 2 панели, `unusedTrack=Внутренний` -> есть текст про внутреннюю полосу.
- 3 рельса, 2 панели, `unusedTrack=Внешний` -> есть текст про внешнюю полосу.
- Полный набор панелей -> текста нет.

#### 4. Производственный лист: порог, дренаж и картинка накладного порога

Проблемы:

- Для порога нужно добавить/сохранить заметку `рассверлить дренажные отверстия` там, где это требуется.
- Если выбран накладной анод или накладной окраш, картинка порога должна быть другая.
- В названии желательно явно писать `Порог 3-рельсовый накладной` / `Порог 5-рельсовый накладной`.

Текущее наблюдение:

- В каталоге есть `RS23231` и `RS23251`, но сейчас они ссылаются на картинки стандартных порогов (`RS2323.jpg` / `RS1325.jpg`).
- В `assets/profiles` нет отдельных файлов `RS23231` и `RS23251`.

Ожидаемая правка:

- В `profile_catalog.py` и/или расчетной сборке профиля довести название накладного порога до понятного вида.
- Для накладного порога добавить отдельные картинки в `Raluma/backend/assets/profiles`, если берем их из нового DOCX/техкаталога.
- Не подменять картинку стандартного порога накладной картинкой.
- Сохранить правило по дренажу и проверить, где оно должно быть: стандарт/накладной, 3/5 рельсов.

Проверки:

- Стандартный 3-рельсовый порог -> стандартная картинка и стандартное название.
- Накладной 3-рельсовый порог -> накладная картинка и слово `накладной`.
- Аналогично для 5 рельсов.

#### 5. Производственный лист: увеличить/улучшить картинку стекольного профиля `RS2021`

Проблема:

- Картинка стекольного профиля плохо читается, место в таблице есть.

Ожидаемая правка:

- В `section_sheet.html` увеличить область картинки `RS2021`.
- Не ломать таблицу: нарезка стекольного профиля должна остаться компактной и не разъехать страницу.

Проверки:

- HTML preview для 1 ряда с `RS2021` визуально читаемый.
- PDF/print layout не съехал.

#### 6. Производственный лист: зеркалирование межстекольного профиля на схеме сверху

Проблема:

- Межстекольный профиль клеится начиная с первой панели изнутри помещения.
- Сейчас по замечанию он нарисован не той стороной; картинка должна зеркалиться.
- На 3-панельном примере профиль должен оказаться там, где на скрине зеленые кружки.

Ожидаемая правка:

- Проверить отдельно 1 ряд: правило зависит от `firstPanelInside`.
- Для `RS2061/RS1006` на схеме сверху не просто ставить один и тот же `П`-образный маркер, а учитывать направление первой панели.
- Не переносить старое поле `firstPanelInside` на 2 ряда: для 2 рядов это уже выражается через `Первые панели: в центре/снаружи`.

Проверки:

- 1 ряд, первая панель справа -> профиль смотрит в одну сторону.
- 1 ряд, первая панель слева -> профиль зеркалится.
- 3 панели с межстекольным профилем -> визуально соответствует замечанию.
- 2 ряда не ломаются.

#### 7. Форма секции: убрать переключатель `Система 1 ряд / 2 ряда`

Статус: реализовано.

Проблема:

- В окне добавления выбирается `СЛАЙД стандарт 1 ряд` или `СЛАЙД стандарт 2 ряда`.
- После выбора внутри формы снова показывается переключатель `Система: 1 ряд / 2 ряда`.
- По замечанию его нужно убрать из формы слайда.

Ожидаемая правка:

- В `FormTabs.tsx` убрать/скрыть переключатель `slideRows`.
- Оставить создание 1 ряда и 2 рядов через боковой `+`.
- Если позже надо менять тип уже созданной секции, это отдельный UX-вопрос, сейчас не делаем.
- Оставить без изменений:
  - выбор `3х / 5ти рельсовая`;
  - количество панелей;
  - `unusedTrack`: для 1 ряда как `Неиспользуемый рельс`, для 2 рядов как `Первые панели`;
  - центральную фурнитуру для 2 рядов.

Проверки:

- Создание `Стандарт 1 ряд` все еще создает 1 ряд.
- Создание `2 ряда от центра` все еще создает 2 ряда.
- Внутри формы переключателя `1 ряд / 2 ряда` нет.
- Переключатель `3х/5ти рельсовая` остается.
- Панельные варианты и схемы для 1 ряда и 2 рядов остаются корректными.

Что сделано:

- В `Raluma/src/components/editor/FormTabs.tsx` удален только блок формы `Система` с переключателем `1 ряд / 2 ряда`.
- `slideRows` не пересчитывается из формы редактирования и остается тем, что было выбрано при создании секции.
- Остались рабочими:
  - выбор `3х / 5ти рельсовая`;
  - количество панелей;
  - `Неиспользуемый рельс` для 1 ряда;
  - `Первые панели` для 2 рядов;
  - центральная фурнитура 2 рядов.
- В `Raluma/scripts/manual-qa-2row.mjs` добавлены проверки `one_row_no_row_switch` и `two_row_no_row_switch`.

Проверено:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed, остались только 14 старых warnings.
- `npm.cmd run build` - passed, остался стандартный Vite warning про размер chunk.
- `node scripts/manual-qa-2row.mjs http://localhost:5173` - passed:
  - новая секция `Стандарт 1 ряд` создается;
  - новая секция `2 ряда от центра` создается;
  - в форме 1 ряда нет переключателя рядности;
  - в форме 2 рядов нет переключателя рядности;
  - 2 ряда сохраняют `3х рельсовая`, `4 панели`, `Первые панели`;
  - производственный лист 1 ряда и 2 рядов открывается.

#### 8. Кнопка копирования секции

Проблема:

- Менеджерам нужна быстрая кнопка копирования секции в карточке секции слева.

Ожидаемая правка:

- Добавить кнопку копирования рядом с удалением в `EditorSidebar.tsx`.
- Реализовать handler в `ProjectEditor.tsx`.
- Копия должна:
  - получить новое имя типа `Секция N`;
  - скопировать все параметры секции;
  - не копировать `id`, `project_id`, `order`;
  - не копировать ручные правки производственного листа `document_overrides`;
  - создаваться через существующий `createSection`, чтобы работало и для сервера, и для guest/local режима.

Проверки:

- Копирование авторизованного проекта.
- Копирование гостевого/local проекта.
- Копия сразу выбирается в редакторе.
- Удаление по-прежнему работает и кнопки не конфликтуют.

#### 9. Схема сверху: боковые профили должны отображаться схематично как в производственном листе

Проблема:

- Нужно, чтобы боковые профили были видны на виде сверху: не обязательно идеально в масштабе, но важно показывать сам факт и сторону профиля.
- На скрине отмечены пристеночные/боковые профили по краям.

Ожидаемая правка:

- В UI-схеме `SlideDiagrams.tsx` уже есть упрощенная отрисовка бокового stack, но ее надо сверить и довести под требование.
- В `section_sheet.html` схема сверху должна тоже показывать боковые профили, а не только тонкие вертикальные ограничители.
- Для пристеночного профиля сохранять 16 мм.
- Для `RS2081/RS1082/RS112/RS1002` показывать схематично понятный блок/контур на нужной стороне.

Проверки:

- Профиль слева виден слева.
- Профиль справа виден справа.
- Если профилей нет, лишние боковые элементы не появляются.
- Вид сверху в preview и производственном листе согласован по смыслу.

### Рекомендуемый порядок реализации

1. Малые backend/template правки производственного листа: цвет, скрытие нулевого межстекольного, текст про неиспользуемую полосу.
2. Порог: названия, notes, отдельные картинки накладного порога.
3. `RS2021`: увеличить картинку в производственном листе.
4. UI: убрать переключатель `1 ряд / 2 ряда` из формы секции. Выполнено.
5. UI/API-flow: добавить копирование секции через существующий `createSection`.
6. Схемы сверху: зеркалирование межстекольного и боковые профили.
7. Тесты и ручная проверка:
   - `pytest -q`
   - `ruff check` по измененным backend-файлам
   - `npm.cmd run typecheck`
   - `npm.cmd run lint`
   - `npm.cmd run build`
   - ручная проверка 1 ряд/2 ряда в браузере и preview производственного листа.

### 4. Зеркалирование межстекольного профиля

Статус: перенесено в новый пакет правок `ПРАВКИ 12.06.26.docx`.

Ранее:

- На проверенных скриншотах выбран режим `Без межстекольного профиля`.
- Нельзя делать вывод о зеркалировании без включенного `RS2061` или `RS1006`.

Теперь:

- По новым скринам и пояснению проблема подтверждена.
- Исправление запланировано в пункте 6 нового пакета.

### 5. Project-level плейсхолдеры `Спецификация` и `Накладная`

Статус: отложено по решению пользователя.

Решение:

- Пока оставляем кнопки как есть.
- Пользователь позже либо доделает эти документы, либо уберет кнопки.

## Снято или не делаем сейчас

### 6. Вертикальные палки на схеме сверху

Статус: вопрос снят пользователем.

Решение:

- Текущий вид по этому пункту принимаем.
- Код не трогаем.
- Возвращаться только при новом конкретном скриншоте/сценарии.

### 7. Excel-выгрузки

Статус: не делаем как целевой формат.

Решение:

- Для заказа стекла и заявки на покраску держим направление HTML-preview и PDF.

## Следующие большие этапы

### 8. HTML/PDF заказ стекла

Статус: позже.

Сохранить направление:

- Не Excel.
- Оставляем HTML-preview и PDF.
- `(чертеж)` уже считается на физических стеклах.
- Остается довести финальный формат/визуал, маркировку и требования к предупреждениям после следующего просмотра заказчиком.

### 9. HTML/PDF заявка на покраску

Статус: позже.

Сохранить направление:

- Не Excel.
- Оставляем HTML-preview и PDF.
- Группировать по цветам.
- Включать артикул, картинку, количество, чистовой размер, припуск 50 мм и погонные метры.

### 10. Каталог как источник расчета

Статус: большой отдельный этап.

Сохранить направление:

- Каталог должен стать источником параметров, цветов, цен и картинок.
- Нужны варианты внутри артикула: цвет, цена, наценка, красится/не красится, анод/не анод, активность, картинка/примечание.
- Статический каталог оставить как seed/fallback, но не как единственный источник истины.

### 11. Точная масштабная схема с профилями

Статус: последний большой этап.

Сохранить направление:

- Общая геометрия проема.
- В одной шкале учитывать ширину конструкции, стекла, боковые профили, пристеночные профили, порог, верхний профиль и перехлесты.
- Использовать одну геометрию для UI и производственного листа.

## Статус CODEX1

Ближайшие согласованные правки 1-4 выполнены.

Остались отложенные и большие этапы: проверка зеркалирования межстекольного профиля, документы HTML/PDF, каталог как источник расчета и точная масштабная схема.

Перед следующим кодовым этапом снова сначала согласовать конкретный пункт, затем делать узкую реализацию с тестами.
